/** Sandbox widget. The server route uses the same origin so the session matches the frame. */
export const ONRAMP_WIDGET_BASE_URL =
  process.env.NEXT_PUBLIC_ONRAMP_WIDGET_BASE_URL?.trim() || "https://onramp-sandbox.arc.io";
